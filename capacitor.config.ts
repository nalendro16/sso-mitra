import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.sajang.mitra',
  appName: 'Mitra Satu Pintu',
  webDir: 'build',
  server: {
    androidScheme: 'https',
  },
}

export default config
